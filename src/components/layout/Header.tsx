import React from 'react';
import Nav from './Nav';

const Header: React.FC = () => {
    return (
        <header className="header">
            <Nav />
        </header>
    );
};

export default Header;
